# 🔑 GitHub 个人访问令牌配置指南

## 📋 创建个人访问令牌步骤

### 1. 登录 GitHub 并访问设置页面
- 🔗 访问：https://github.com/settings/tokens

### 2. 生成新的令牌
1. 点击 **"Generate new token"** → **"Generate new token (classic)"**
2. 填写令牌信息：
   - **Note**: `DevFest Workshop - Local Development`
   - **Expiration**: 选择合适的过期时间（建议 30 天或 90 天）

### 3. 选择权限范围 (Scopes)
请勾选以下权限：
- ✅ **repo** (完整的仓库访问权限)
  - ✅ repo:status
  - ✅ repo_deployment
  - ✅ public_repo
  - ✅ repo:invite
  - ✅ security_events
- ✅ **workflow** (GitHub Actions 工作流权限)
- ✅ **write:packages** (如果需要发布包)
- ✅ **read:org** (读取组织信息)

### 4. 生成并复制令牌
1. 点击 **"Generate token"**
2. **⚠️ 重要：立即复制令牌并保存**（页面刷新后将无法再次查看）

## 🚀 使用令牌推送代码

### 方法一：在推送时输入凭据
```bash
cd /Users/cyx/Desktop/devtools-mcp-website
git push -u origin main
```
当提示输入用户名和密码时：
- **Username**: `iccyyxx`
- **Password**: `粘贴您刚才复制的个人访问令牌`

### 方法二：在 URL 中包含令牌（临时使用）
```bash
git remote set-url origin https://iccyyxx:YOUR_TOKEN@github.com/iccyyxx/devtools-mcp-website.git
git push -u origin main
```

### 方法三：使用 Git 凭据管理器
```bash
# 配置 Git 记住凭据
git config --global credential.helper store

# 然后正常推送，输入一次后会自动保存
git push -u origin main
```

## 🔧 推送命令

准备好令牌后，执行以下命令：

```bash
# 进入项目目录
cd /Users/cyx/Desktop/devtools-mcp-website

# 检查状态
git status

# 添加所有文件
git add .

# 提交更改
git commit -m "🎉 完整的 DevFest Workshop 网站 - 包含所有页面和功能"

# 推送到 GitHub（会提示输入用户名和令牌）
git push -u origin main
```

## 📝 注意事项

1. **安全性**：
   - 个人访问令牌等同于密码，请妥善保管
   - 不要在代码中硬编码令牌
   - 定期更新令牌

2. **权限**：
   - 只授予必要的权限
   - 为不同用途创建不同的令牌

3. **过期管理**：
   - 设置合理的过期时间
   - 过期前及时续期

---

🎯 **下一步**：创建令牌后，告诉我您已经准备好，我会帮您执行推送命令！
